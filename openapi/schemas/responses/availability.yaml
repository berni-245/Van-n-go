description: >
  - `zones`: array of zone URIs where a vehicle works

  - `timeSlots`: array of time slots, representing a day and time on which a vehicle works
type: object
properties:
  zones:
    type: array
    items:
      type: string
    example: ["api/zones/1", "api/zones/2", "api/zones/3"]
  timeSlots:
    type: array
    items:
      $ref: "week-time-slot.yaml"
