type: object
required: [id, name]
properties:
  # I don't think it's necessary if we don't expose a province/neighborhood endpoint.
  # id:
  #   type: integer
  name:
    type: string
    example: Palermo
