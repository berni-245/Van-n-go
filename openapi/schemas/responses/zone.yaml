type: object
required: [id, country, province, neighborhood]
properties:
  id:
    type: integer
  country:
    $ref: "country.yaml"
  province:
    $ref: "province.yaml"
  neighborhood:
    $ref: "neighborhood.yaml"
